import {View, Text} from 'react-native';
import React from 'react';

const VersionHistory = () => {
  return (
    <View>
      <Text>VersionHistory</Text>
    </View>
  );
};

export default VersionHistory;
